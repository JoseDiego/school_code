<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span9">
  	<div class="tabbable"> 
		<ul class="nav nav-tabs">
			<li class="active"><a href="#perfil" data-toggle="tab">Perfil</a></li>
			<li><a href="#avances" data-toggle="tab">Avances</a></li>
			<li><a href="#retos" data-toggle="tab">Retos</a></li>
			<li><a href="#mensajes" data-toggle="tab">Mensajes</a></li>
		</ul>
		<div class="tab-content">
				<div class="tab-pane active" id="perfil">
					<%= render 'users/profile' %>
				</div>
				<div class="tab-pane" id="avances">
					<%= render 'users/progress' %>
				</div>
				<div class="tab-pane" id="retos">
					<%= render 'users/challengs' %>
				</div>
				<div class="tab-pane" id="mensajes">
					<%= render 'users/messages' %>
				</div>
		</div>
	</div>
  </aside>
</div>